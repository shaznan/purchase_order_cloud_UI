import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  groupItem: { paddingBottom: "1.5rem" },
  item: {
    color: "red !important",
    height: "0rem",
    marginBottom: "1.2rem",
    fontSize: "0.7rem",
    // color: "black",
    fontWeight: 500,
    // padding: "1rem",
  },
}));

export { useStyles };
